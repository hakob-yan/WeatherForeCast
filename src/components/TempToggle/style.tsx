import styled from "styled-components";
import { colors } from "../../constants/Color";

export const Temp = styled.form`
  position: absolute;
  align-self: flex-end;
  margin: 0 2vw;
  background-color: ${colors.White};
  padding: 5px;
  border-radius:20px;
`;
